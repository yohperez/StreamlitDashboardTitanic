version: '3.8'

services:
  streamlit:
    build: .
    container_name: streamlit-titanic-dashboard
    ports:
      - "8501:8501"
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
      - STREAMLIT_LOGGER_LEVEL=info
    volumes:
      - ./data:/app/data
    restart: unless-stopped
